- **Versioning:** EVERY time code is changed and committed, YOU MUST bump the version number in `package.json` using `npm version patch --no-git-tag-version` and update the version displayed in the UI (e.g. in `src/App.tsx` or `LandingPage.tsx`).
